<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ruta para archivos en el directorio de archivos temporales de la aplicación -->
    <files-path
        name="files"
        path="." />
    <!-- Ruta para archivos en el directorio de caché de la aplicación -->
    <cache-path
        name="cache"
        path="." />
    <!-- Ruta para archivos en el directorio específico de rentingPDF -->
    <files-path
        name="rentingPDF"
        path="." />
    <external-path
        name="external_files"
        path="." />
</paths>
